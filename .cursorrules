# Cursor Rules - Context Factory Bridge

- Refer to `./orchestrator/SHARED.md` for shared orchestration directives.
- Use `node scripts/context.mjs resolve "<prompt>"` to determine relevant rules and skills.
- Save task plans to `./docs/tasks/` and ADRs to `./docs/decisions/`.

# Context Factory Entry Point

Read `orchestrator/SHARED.md` before changing this factory or using it to guide project work. Treat `context-manifest.json` as the canonical inventory, load only task-relevant rules and skills, and run `node scripts/validate-context.mjs` after context changes.
